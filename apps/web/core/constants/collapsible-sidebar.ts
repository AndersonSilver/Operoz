/** Largura padrão das sidebars de configurações (px). */
export const SETTINGS_SIDEBAR_WIDTH = 250;

/** Atraso antes de fechar o peek ao sair com o mouse (ms). */
export const SIDEBAR_PEEK_CLOSE_DURATION = 600;

/** Faixa invisível na borda esquerda para abrir peek (px). */
export const SIDEBAR_HOVER_STRIP_WIDTH = 12;
