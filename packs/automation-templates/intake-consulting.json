{
  "id": "intake-consulting",
  "version": 1,
  "name": "Recepção — resolvido via consultoria",
  "description": "Notifica o solicitante quando um pedido é encerrado como consultoria, sem gerar card de delivery.",
  "icon": "message-circle",
  "category": "intake",
  "default_rule_name": "Aviso de resolução por consultoria",
  "default_description": "Template oficial Operoz — avisa o solicitante ao encerrar um pedido como consultoria.",
  "parameters": [
    { "key": "rule_name", "type": "string", "label": "Nome da regra", "default": "Aviso de resolução por consultoria" },
    { "key": "template_id", "type": "string", "label": "ID do template de e-mail", "required": true, "default": "" },
    { "key": "recipient_emails", "type": "array", "label": "Destinatários extras (e-mails)", "default": [] }
  ],
  "graph": {
    "nodes": [
      {
        "id": "trigger-consulting",
        "type": "automationNode",
        "position": { "x": 80, "y": 120 },
        "data": {
          "kind": "trigger",
          "catalog_key": "intake.consulting",
          "label": "Recepção — resolvido via consultoria",
          "config": { "event_type": "intake.consulting" }
        }
      },
      {
        "id": "action-email",
        "type": "automationNode",
        "position": { "x": 380, "y": 120 },
        "data": {
          "kind": "action",
          "catalog_key": "action.send_email",
          "label": "Enviar e-mail",
          "config": {
            "template_id": "{{template_id}}",
            "recipient_emails": "{{recipient_emails}}"
          }
        }
      }
    ],
    "edges": [{ "id": "edge-1", "source": "trigger-consulting", "target": "action-email" }]
  }
}
